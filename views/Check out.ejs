<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Aston Martin 336</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <style>
        /* Basic Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #000000;
            height: 100vh; /* Full height of viewport */
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Main Box Styling */
        .swiper-slide {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 30px;
            height: 100%;
        }

        .box {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            width: 350px; /* Fixed width for box */
            text-align: center;
            display: flex;
            flex-direction: column; /* Vertically stack content */
            justify-content: space-between; /* Space between image and text content */
            height: auto; /* Ensure height is flexible */
        }

        .box img {
            width: 100%;
            height: auto;
            border-bottom: 2px solid #f4f4f4;
        }

        .content {
            padding: 20px;
        }

        .content h3 {
            font-size: 24px;
            color: #333;
            margin-bottom: 10px;
        }

        .price {
            font-size: 18px;
            color: #e74c3c;
            margin-bottom: 10px;
        }

        .price span {
            font-weight: bold;
        }

        .content p {
            font-size: 16px;
            color: #555;
            margin-bottom: 15px;
        }

        .btn {
            background-color: #3498db;
            color: white;
            padding: 12px 20px;
            font-size: 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }

        .btn:hover {
            background-color: #2980b9;
        }

        /* Modal Styles */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            justify-content: center;
            align-items: center;
        }

        .modal-content {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            width: 80%;
            max-width: 600px;
            text-align: center;
            animation: fadeIn 0.3s ease-out;
        }

        .modal h2 {
            font-size: 28px;
            margin-bottom: 10px;
        }

        .modal p {
            font-size: 16px;
            color: #333;
            margin-bottom: 10px;
        }

        .modal img {
            width: 100%;
            max-width: 400px;
            margin-bottom: 15px;
        }

        .close {
            background-color: #e74c3c;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 20px;
        }

        .close:hover {
            background-color: #c0392b;
        }

        /* Fade-in Animation */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        /* Media Queries for Responsiveness */
        @media (max-width: 768px) {
            .swiper-slide {
                flex-direction: column;
                align-items: center;
            }

            .box {
                width: 90%;
            }

            .modal-content {
                width: 95%;
            }
        }
    </style>
</head>
<body>

    <!-- Main Content -->
    <div class="swiper-slide box">
        <img src="image/AM 1.jfif.jpg" alt="Aston Martin 336">
        <div class="content">
            <h3>ASTON MARTIN 336</h3>
            <div class="price"> <span>price : </span> ₹9,00,000/- </div>
            <p>
                new
                <span class="fas fa-circle"></span> 1960
                <span class="fas fa-circle"></span> Automatic
                <span class="fas fa-circle"></span> petrol
                <span class="fas fa-circle"></span> 183mph
            </p>
            <a href="car booking.html" class="btn" id="checkOutBtn">Check Out</a>
        </div>
    </div>

    <!-- Modal -->
    <div id="carModal" class="modal">
        <div class="modal-content">
            <h2>Aston Martin 336 Details</h2>
            <img src="image/AM 1.jfif.jpg" alt="Aston Martin 336">
            <p><strong>Price:</strong> ₹9,00,000/-</p>
            <p><strong>Model Year:</strong> 1960</p>
            <p><strong>Transmission:</strong> Automatic</p>
            <p><strong>Fuel Type:</strong> Petrol</p>
            <p><strong>Top Speed:</strong> 183mph</p>
            <p><strong>Description:</strong> 
                The Aston Martin 336 is a true masterpiece of automotive history, blending British elegance with exceptional performance. Under the hood, it boasts a 2.9-liter straight-six engine, delivering a robust 150 horsepower and 180 Nm of torque, allowing the car to reach impressive speeds of up to 183 mph. This power is matched by its finely-tuned automatic transmission, offering smooth shifts and responsive handling, even by today’s standards. Despite its vintage 1960 model year, the Aston Martin 336 remains a symbol of luxury and precision engineering, providing a remarkable balance of performance and refinement.</p>
            <button class="close" id="closeModal">Close</button>
        </div>
    </div>

    <!-- JavaScript -->
    <script>
        // Get modal element and buttons
        const modal = document.getElementById('carModal');
        const checkOutBtn = document.getElementById('checkOutBtn');
        const closeModalBtn = document.getElementById('closeModal');

        // Show the modal when "Check Out" button is clicked
        checkOutBtn.addEventListener('click', function(event) {
            event.preventDefault();  // Prevent the default anchor behavior
            modal.style.display = 'flex';
        });

        // Close the modal when "Close" button is clicked
        closeModalBtn.addEventListener('click', function() {
            modal.style.display = 'none';
        });

        // Close the modal if user clicks anywhere outside the modal
        window.addEventListener('click', function(event) {
            if (event.target === modal) {
                modal.style.display = 'none';
            }
        });
    </script>

</body>
</html>
